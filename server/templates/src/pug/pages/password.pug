extends ../layouts/authentication.pug

block config
    - var pageTitle = 'Login'

block content
    .container
        .row.justify-content-center
            .col-lg-5
                .card.shadow-lg.border-0.rounded-lg.mt-5
                    .card-header
                        h3.text-center.font-weight-light.my-4 Choose a Password
                    .card-body.text-center
                        .small
                          p (I am not paying for super high web security, so don't use your bank password, 'kay?)
                        form(action='{{ data["nextPage"] }}', method='POST')
                            .form-floating.mb-3
                                input#inputPassword.form-control(type='password', placeholder='', name='password', value='')
                                label(for='inputPassword') Password
                            .mt-4.mb-0
                                .d-grid
                                    input.btn.btn-primary.btn-block(type='submit', value='Create Account')
                    .card-footer.text-center
                      .small
                        | By using this service, you agree to the
                        a(href='/terms.html') Terms and Services
